# Measurable Execution Rules
<!-- 
  PURPOSE: Define enforceable, QUANTITATIVE coding rules for the project.
  WHEN TO UPDATE: When new thresholds are agreed upon or existing ones change.
  
  FORMAT GUIDE:
  - Rules MUST be measurable — include numbers, limits, and specific locations
  - Vague rules like "write clean code" are NOT allowed
  - Each rule should be independently verifiable
  
  EXAMPLE:
  1. No single component file shall exceed 150 lines.
  2. No function shall accept more than 4 parameters (use object destructuring).
  3. Every shared utility must live in `/src/lib/` — never inline.
  4. All database queries must go through `/src/lib/db.ts` — no direct Supabase calls in components.
  5. No `any` types in TypeScript. Use strict mode.
-->
**Last-Modified-By:** — | **Last-Modified-Session:** —

<!-- Add your measurable rules below -->


## Changelog
<!-- Format: - [Session #] Description of what changed. -->
